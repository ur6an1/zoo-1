[project]
name = "zoo-bot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "aiogram>=3.26",
    "httpx>=0.27",
    "redis>=5.0",
    "zoo-shared",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
