[project]
name = "zoo-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "aiogram>=3.26",
    "APScheduler>=3.10",
    "aiohttp>=3.9",
    "zoo-shared",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
